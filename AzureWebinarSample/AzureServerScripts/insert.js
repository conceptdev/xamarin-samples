function insert(item, user, request) 
{
    request.execute();
    
//    if (item.text.length < 2){
//        request.respond(statusCodes.BAD_REQUEST, 'didn\'t you mean to type more?')
//    } else {
//        item.userId = user.userId;
//        request.execute();
//    }
}